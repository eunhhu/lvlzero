import {FC} from 'react'

const ProfileState:FC<{lang:string; user:IUser}> = ({lang, user}) => {
    return <div className="flex flex-col justify-center items-center">
        <div className=""></div>
    </div>
}

export default ProfileState